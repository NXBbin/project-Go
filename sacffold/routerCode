	//生成路由代码，脚手架模板
	//品牌 Restful 路由
	r.GET("/brand", controller.BrandList)

	r.DELETE("/brand", controller.BrandDelete)
	
	r.POST("/brand", controller.BrandCreate)

	r.PUT("/brand", controller.BrandUpdate)	//生成路由代码，脚手架模板
	//品牌 Restful 路由
	r.GET("/brand", controller.BrandList)

	r.DELETE("/brand", controller.BrandDelete)
	
	r.POST("/brand", controller.BrandCreate)

	r.PUT("/brand", controller.BrandUpdate)	//生成路由代码，脚手架模板
	//用户 Restful 路由
	r.GET("/user", controller.UserList)

	r.DELETE("/user", controller.UserDelete)
	
	r.POST("/user", controller.UserCreate)

	r.PUT("/user", controller.UserUpdate)	//生成路由代码，脚手架模板
	//用户 Restful 路由
	r.GET("/user", controller.UserList)

	r.DELETE("/user", controller.UserDelete)
	
	r.POST("/user", controller.UserCreate)

	r.PUT("/user", controller.UserUpdate)	//生成路由代码，脚手架模板
	//用户 Restful 路由
	r.GET("/user", controller.UserList)

	r.DELETE("/user", controller.UserDelete)
	
	r.POST("/user", controller.UserCreate)

	r.PUT("/user", controller.UserUpdate)	//生成路由代码，脚手架模板
	//角色 Restful 路由
	r.GET("/role", controller.RoleList)

	r.DELETE("/role", controller.RoleDelete)
	
	r.POST("/role", controller.RoleCreate)

	r.PUT("/role", controller.RoleUpdate)	//生成路由代码，脚手架模板
	//角色 Restful 路由
	r.GET("/role", controller.RoleList)

	r.DELETE("/role", controller.RoleDelete)
	
	r.POST("/role", controller.RoleCreate)

	r.PUT("/role", controller.RoleUpdate)	//生成路由代码，脚手架模板
	//权限 Restful 路由
	r.GET("/privilege", controller.PrivilegeList)

	r.DELETE("/privilege", controller.PrivilegeDelete)
	
	r.POST("/privilege", controller.PrivilegeCreate)

	r.PUT("/privilege", controller.PrivilegeUpdate)	//生成路由代码，脚手架模板
	//角色 Restful 路由
	r.GET("/role", controller.RoleList)

	r.DELETE("/role", controller.RoleDelete)
	
	r.POST("/role", controller.RoleCreate)

	r.PUT("/role", controller.RoleUpdate)	//生成路由代码，脚手架模板
	//属性类型 Restful 路由
	r.GET("/attr-type", controller.AttrTypeList)

	r.DELETE("/attr-type", controller.AttrTypeDelete)
	
	r.POST("/attr-type", controller.AttrTypeCreate)

	r.PUT("/attr-type", controller.AttrTypeUpdate)	//生成路由代码，脚手架模板
	//属性分组 Restful 路由
	r.GET("/attr-group", controller.AttrGroupList)

	r.DELETE("/attr-group", controller.AttrGroupDelete)
	
	r.POST("/attr-group", controller.AttrGroupCreate)

	r.PUT("/attr-group", controller.AttrGroupUpdate)	//生成路由代码，脚手架模板
	//属性分组 Restful 路由
	r.GET("/attr-group", controller.AttrGroupList)

	r.DELETE("/attr-group", controller.AttrGroupDelete)
	
	r.POST("/attr-group", controller.AttrGroupCreate)

	r.PUT("/attr-group", controller.AttrGroupUpdate)	//生成路由代码，脚手架模板
	//属性 Restful 路由
	r.GET("/attr", controller.AttrList)

	r.DELETE("/attr", controller.AttrDelete)
	
	r.POST("/attr", controller.AttrCreate)

	r.PUT("/attr", controller.AttrUpdate)	//生成路由代码，脚手架模板
	//产品属性 Restful 路由
	r.GET("/product-attr", controller.ProductAttrList)

	r.DELETE("/product-attr", controller.ProductAttrDelete)
	
	r.POST("/product-attr", controller.ProductAttrCreate)

	r.PUT("/product-attr", controller.ProductAttrUpdate)